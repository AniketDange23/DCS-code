let input = "JAvaScript"; // Declare a variable to store the input string
let index = 1; // Declare a variable to store the index of the character to check
let isUpper = input.charAt(index).toUpperCase() === input.charAt(index); // Check if the character at the provided index is uppercase
console.log(isUpper); // Output the result (true if uppercase, false if not)
